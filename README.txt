Kennedy Concessions — Official School Logo Update

SAFE UPDATE:
Upload the included assets folder into the root of the existing GitHub repository.

This replaces:
assets/logo-left.png

Nothing else is changed:
- menu-data.js is untouched
- index.html is untouched
- editor.html is untouched
- products, prices, images, sponsors, stock status, and ordering remain unchanged

The supplied school logo was cleaned to a transparent background and sized for crisp display on the 55-inch 4K TV.
